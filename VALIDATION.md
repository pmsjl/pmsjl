# 本地验收记录 · 2026-09-07

- README 正文与工作区镜像一致；所有本地图片路径、SVG XML 及尺寸属性检查通过。
- GitHub 风格 HTML 在 1440px / 375px、深色 / 浅色四种组合下无页面横向溢出、无缺图；技术表格在手机宽度内可显示。代码块保留水平滚动能力。
- 横幅、打字、流光分隔线和页脚包含 CSS / SMIL 动画；横幅与打字已替换为参考同款服务输出。按三个句子的完整显示时间点检查交替显示，无同时重叠。
- 减少动效模式通过 picture 选中静态 SVG；各自制 SVG 内的减少动效规则也单独检查通过。正常模式下嵌入图片的前后帧不同。
- 工作流 YAML 解析通过，核对 UTC 20:17 调度、写入权限和目标仓库限制。
- 发布脚本通过“缺失图片”“无效 SVG”“完整有效批次”三个场景；前两种情况不覆盖旧图片。
- 贡献城市保留 3D 提交视图、422 contributions 与语言占比；Issues、PR、Review 雷达和零值图标已移除，语言圆环移动到右上空位且不遮挡提交城市。
- 此前已检查在线体验、作品集及项目仓库地址，均返回 200；本次仅删减重复入口，未新增目标地址。邮箱地址与当前简历一致，未发送验证邮件。

尚未执行：创建/更新远程个人主页仓库、GitHub 页面实际渲染。本地贡献素材来自已生成结果；账户可见贡献范围及 GitHub 图片缓存仍需发布后确认。

预览的 CSS 只用于本地查看，不会改变 GitHub 自身的样式。贡献动画由工作流生成，自制素材由 tools/build_assets.py 生成。
